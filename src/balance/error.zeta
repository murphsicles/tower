// Auto-converted from /tmp/df-tower-0.5.3/tower-0.5.3/src/balance/error.rs

use std::fmt;
/// The balancer's endpoint discovery stream failed.
#[derive(Debug)]
pub struct Discover {
 (crate::BoxError),
}

impl fmt::Display for Discover {
 fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
 write!(f, "load balancer discovery error: {}", self.0)
 }
 
}

impl std::error::Error for Discover {
 fn source(&self) -> Option<&(_)> {
 Some(&*self.0)
 }
 
}

