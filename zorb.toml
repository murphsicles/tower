[package]
name = "@net/tower"
version = "0.5.3"
description = "Service/middleware abstraction for Zeta — Tower Service, Layer, Buffer, Retry, Timeout, Load"
license = "MIT"
repository = "https://github.com/murphsicles/tower"
readme = "README.md"
